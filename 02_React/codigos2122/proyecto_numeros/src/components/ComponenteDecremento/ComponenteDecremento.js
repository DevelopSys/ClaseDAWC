import React from "react";

const ComponenteDecremento = ({ counter }) => {
  return (
    <div>
      <button className="btn btn-primary" id="btn_decremento">
        -1
      </button>
    </div>
  );
};

export default ComponenteDecremento;
