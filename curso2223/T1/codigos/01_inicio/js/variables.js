if (true) {
  let numero = 20;
  var numeroScope = 30;
  console.log(`Impresión de la variable let dentro del bloque ${numero}`);
}

console.log(`Impresión de la variable let fuera del bloque ${numero}`);
console.log(`Impresión de la variable let fuera del bloque ${numeroScope}`);
